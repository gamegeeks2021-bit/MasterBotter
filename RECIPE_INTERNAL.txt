RECIPE_INTERNAL.txt (RECONSTRUCTED UNDER OVERRIDE)
================================================

Build: D.7

This file was reconstructed under the owner-authorized Integrity Override Protocol.
It may not match historical D1 byte-for-byte.

Core Objectives
---------------
- Master Bot with pack-based architecture.
- Pokétwo gameplay surfaced via slash commands + Component V2 UI.
- Move away from !p prefix usage; keep legacy only as fallback.
- Highest priority: GitHub PUSH sync (code-only snapshot) to avoid re-upload loops.
- No manual paste/clipboard workflows required.

Component V2 Standard
---------------------
All UIs follow a consistent embed + components pattern.

POKÉTWO HUB UI (COMPONENT V2)
----------------------------
Format:
TITLE
Description
---separator---
Button description [Button]
---separator---
Button description [Button]
---separator---
Button description [Button]

Starter UI (Generation)
-----------------------
Due to Discord component limits (5 rows), starter selection uses:
- Generation dropdown
- Then 3 starter buttons for chosen generation

User Messaging:
- After picking, notify: can change starter within same day OR until starter levels up.

AUTO GITHUB SYNC (PUSH-ONLY, CODE-ONLY)
--------------------------------------

Goal:
- The bot must be able to PUSH the current codebase to GitHub on demand.
- This is NOT a pull/deploy flow.
- GitHub is treated as a "code mirror" that overwrites old code with the latest snapshot.

Env Files Policy:
- Real runtime secrets must NEVER be uploaded to GitHub.
- Use two env files:

  1) .env
     - REAL secrets used by the bot at runtime (Discord, Mongo, Redis, GitHub token).
     - MUST be excluded from GitHub.

  2) .env.sample
     - Blank template ONLY (keys present, values empty).
     - MUST be included in GitHub so structure can be inspected without guessing.

GitHub Credentials Location:
- Because PebbleHost does not expose a standard Environment Variables UI for this bot type,
  GitHub push credentials must be stored in the REAL .env (excluded from GitHub), not in Git Management.

Required .env.sample keys (blank values):
- BOT_TOKEN=
- POKETWO_DATABASE_URI=
- POKETWO_DATABASE_NAME=
- POKETWO_REDIS_URL=
- GITHUB_REPO=
- GITHUB_BRANCH=main
- GITHUB_TOKEN=

.gitignore rules:
- .env must be ignored
- .env.sample must be allowed

Example:
  .env
  *.env
  !.env.sample

Push Behavior:
- Sync operation must create a CODE-ONLY snapshot and then FORCE PUSH to GitHub.
- Force push is intended: overwrite old repo contents with the latest code snapshot.

Hard Exclusions (never push these):
- .env (and any secrets files)
- data/, sprites/, poketwo_data/, cache/, logs/
- plugins/poketwo/data/, plugins/poketwo/sprites/
- __pycache__/, *.pyc
- db/sqlite files (*.db, *.sqlite)

Safety:
- Before pushing, run a basic secret scan on the snapshot and abort if secrets are detected.
- Never print tokens to logs.

ENVIRONMENT FILE POLICY (MANDATORY STRUCTURE)
---------------------------------------------

Purpose:
To eliminate confusion between runtime secrets and GitHub-visible structure.

Two environment files MUST exist in the project:

1) .env
   - This is the REAL runtime environment file.
   - Contains actual secrets (Discord token, Mongo URI, Redis URL, GitHub token).
   - This file MUST be present in the zip build.
   - This file MUST be excluded from GitHub.
   - This file WILL be overwritten manually by the owner when deploying.
   - This file is required for runtime push functionality.

2) .env.sample
   - This is a BLANK template file.
   - Contains the exact same keys as .env but with EMPTY values.
   - This file MUST be included in GitHub.
   - This file exists strictly for structure inspection and AI validation.
   - This file must never contain real credentials.

.gitignore Rules (non-negotiable):
   .env
   *.env
   !.env.sample

RECIPE INTEGRITY ENFORCEMENT (VERBATIM APPEND RULE)
----------------------------------------------------
(Owner may maintain a master D1 recipe; future edits must append only unless owner invokes override.)

INTEGRITY OVERRIDE PROTOCOL (OWNER AUTHORITY)
---------------------------------------------
Owner may explicitly authorize reconstruction/regeneration if files expire or are lost.


BOOT GITHUB PUSH (FAIL HARD)
----------------------------
Requirement:
- GitHub push must run automatically at bot startup (no manual command required).
- This is PUSH-ONLY, CODE-ONLY.
- If the push fails, the bot must FAIL HARD (abort startup).

Behavior:
- On process start (before connecting to Discord), create a code-only snapshot.
- Run secret tripwire scan.
- Force push snapshot to the configured branch.
- If credentials missing or push errors, raise and stop.

Required runtime .env keys (real .env only, excluded from GitHub):
- GITHUB_REPO=
- GITHUB_BRANCH=
- GITHUB_TOKEN=
